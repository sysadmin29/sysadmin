---
- name: Check Active Directory Replication
  hosts: windows
  gather_facts: no

  tasks:

    - name: Run repadmin to check replication status
      win_shell: repadmin /replsummary
      register: repl_summary
      ignore_errors: yes

    - name: Debug raw repadmin output
      debug:
        var: repl_summary.stdout_lines

    - name: Extract only relevant lines (skip noise)
      set_fact:
        repl_lines: >-
          {{ repl_summary.stdout_lines
             | select('search','m:.*s')
             | list }}

    - name: Debug parsed replication lines
      debug:
        var: repl_lines

    - name: Calculate half as integer
      set_fact:
        half: "{{ (repl_lines | length // 2) | int }}"

    - name: Split into sources and destinations
      set_fact:
        source_lines: "{{ repl_lines[:half] }}"
        dest_lines: "{{ repl_lines[half:] }}"

    - name: Show cleaned replication summary
      debug:
        msg: |
          Source DSA          largest delta    fails/total %%   error
          {% for line in source_lines %}
          {{ line }}
          {% endfor %}

          Destination DSA     largest delta    fails/total %%   error
          {% for line in dest_lines %}
          {{ line }}
          {% endfor %}